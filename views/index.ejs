
  <%- include("header") -%>
<h1>Concert Genie</h1>
<hr>
  <form class="spotify-queries" action="/" method="post">
    <label for="quantity">Number of Artists: </label>
    <input
        id="quantity"
      type="number"
      name="quantity"
      min="1"
      max="50"
      autocomplete="off"
    />
    <label for="location">City Name: </label>
    <input
        id="location"
      type="text"
      name="location"
      required
    />

    <br>
    <label for="range">Time Range: </label>
    <select name="range" id="range">
     <option value="short_term">Short Term</option>
     <option value="medium_term">Medium Term</option>
     <option value="long_term">Long Term</option>
    </select><br>
     <label for="radius">Radius: </label>
    <select name="radius" id="radius">
        <option>None</option>
     <option value="10">10 miles</option>
     <option value="25">25 miles</option>
     <option value="50">50 miles</option>
     <option value="100">100 miles</option>
    </select><br>
    <button type="submit" name="query">Add</button>
  </form>
<hr>

<% for (let i=0; i < topArtistsEvents.length; i++) { %>
    <% if (topArtistsEvents.length > 0) { %>
        <span><%=topArtistsEvents[i].artist %></span>
   <% } %>

<%}%>

<% for (let i=0; i < recommendedEvents.length; i++) { %>
      <% if (recommendedEvents.length > 0) { %>
    <span><%=recommendedEvents[i].artist%></span>
       <% } %>
<%}%>

 <% for (let i = 0; i < Object.keys(topArtists).length; i++){%>
  <p><%= topArtists[i].artist %></p>
  <% if (topArtists[i].image) {%>
  <img src=<%=topArtists[i].image %> >
  <%} %> <br>

  <% } %>
  <%- include("footer") -%>
